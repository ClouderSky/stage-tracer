一个基于 TypeScript + React 技术栈的脚手架。 

(Create TypeScript React apps with no build configuration.) 

```shell
npm install -g yarn stage-tracer

mkdir projectDir && cd projectDir

tracer init
```

