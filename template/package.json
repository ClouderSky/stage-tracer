{
    "name": "<%= name %>",
    "version": "<%= version %>",
    "description": "<%= description %>",
    "private": true,
    "scripts": {
        "build": "tracer build",
        "start": "tracer start",
        "test": "tracer test"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "stage-tracer": "^<%= pkgConfig.version %>"
    }
}
  